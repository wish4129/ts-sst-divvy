{
  "name": "divvy",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "sst dev",
    "deploy": "sst deploy --stage live",
    "test": "cd web && npx vitest run",
    "test:watch": "cd web && npx vitest",
    "test:coverage": "cd web && npx vitest run --coverage",
    "test:src": "vitest run --config vitest.config.src.ts"
  },
  "devDependencies": {
    "@tsconfig/node18": "^18.2.6",
    "@types/aws-lambda": "^8.10.161",
    "sst": "^3.17.38",
    "typescript": "^5.7.3",
    "vitest": "^4.1.8"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.106.2",
    "drizzle-kit": "^0.30.6",
    "drizzle-orm": "^0.38.4",
    "postgres": "^3.4.9"
  }
}
